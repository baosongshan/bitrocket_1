C语言：
指针:
       const int *p,   int const *p,   int * const  p,    const  int * const p,
       函数指针  指针函数  数组指针  指针数组   --> 由这两组概念构成的复杂指针    int(*(*fun)[5])(void(*)(int))
结构体：
      字节对齐（内存对齐）的计算
      位域
      柔性数组
大小端：
      概念
      应用
宏：
      宏定义：要多加括号
     宏的计算： 不要着急展开
------------------------------------------------------------------------------------------------------------------------
C++
